# BookClub Web API

Simple ASP.NET Core Web API using Entity Framework Core, LINQ and `bookclubdb.sql` schema.

Quick start:

1. Install .NET 8 SDK.
2. Make sure SQL Server is online and database script is executed.
3. Open the project folder in Visual Studio and run it.

Update the connection string in `appsettings.json` if needed.
